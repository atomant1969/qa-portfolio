# CI/CD Testing Portfolio Project

Standalone CI/CD validation examples. This directory contains GitHub Actions workflows plus tests that validate important workflow expectations.

```powershell
cd python\11-ci-cd
pip install -r requirements.txt
pytest
```
