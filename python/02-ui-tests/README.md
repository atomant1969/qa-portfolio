# UI Testing Portfolio Project

Standalone Playwright Python UI test project using local HTML so the examples are stable and runnable.

```powershell
cd python\02-ui-tests
pip install -r requirements.txt
playwright install chromium
pytest
```
