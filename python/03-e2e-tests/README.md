# E2E Testing Portfolio Project

Standalone API plus UI end-to-end example. The test creates state through an API-style service object and validates that the UI renders the same state.

```powershell
cd python\03-e2e-tests
pip install -r requirements.txt
playwright install chromium
pytest
```
